title: Relative Reference Schema
type: object
properties:
  person:
    type: object
    properties:
      name:
        type: string
        description: The name of the person
      address:
        $ref: "definitions/address.schema.yaml"
    required:
      - name
  company:
    type: object
    properties:
      name:
        type: string
        description: The name of the company
      location:
        $ref: "definitions/address.schema.yaml"
    required:
      - name
      - location
